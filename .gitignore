node_modules/
.env
dist/
.vscode/
.DS_Store
tailgrids
back/scripts/
render-sql-backup/
# Documentation
# (Tracked in git so feature plans + implementation guides stay consistent across machines)
# If you need private notes, prefer putting them under docs/private/ and ignoring that subfolder.
# docs/private/
.aider*
scripts/
